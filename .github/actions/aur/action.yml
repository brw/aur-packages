name: "AUR package builder"
description: ""
author: "brw"
branding:
  icon: "package"
  color: "blue"

inputs:
  pkgname:
    description: ""
    required: true

runs:
  using: docker
  image: "docker://ghcr.io/brw/aur-packages/aur-action:latest"
  args:
    - ${{ inputs.action }}
    - ${{ inputs.pkgname }}
